<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <global-method-security proxy-target-class="true" pre-post-annotations="enabled" secured-annotations="enabled" />

    <http pattern="/robots.txt" security="none"/>
    <http pattern="/crossdomain.xml" security="none"/>
    <http pattern="/favicon.ico" security="none"/>
    <http pattern="/apple-touch-*.png" security="none"/>
<!-- <http pattern="/signupflow/**" security="none"/> -->	

    <http pattern="/resources/**" security="none"/>

    <http pattern="/resources.json" security="none"/>

    <http pattern="/error/**" security="none"/>

    <!-- <http pattern="/home" security="none"/> --> 
    <http pattern="/about" security="none"/>
    <http pattern="/contact" security="none"/>

    <http pattern="/**" use-expressions="true">
    <!-- Partie récupérée de l'exemple -->
    	<intercept-url pattern="/favicon.ico" access="permitAll" />
		<intercept-url pattern="/resources/**" access="permitAll" />
		<intercept-url pattern="/auth/**" access="permitAll" />
		<intercept-url pattern="/signin/**" access="permitAll" />
		<intercept-url pattern="/signup/**" access="permitAll" />
		<intercept-url pattern="/disconnect/facebook" access="permitAll" />
		
        <intercept-url pattern="/login" access="isAnonymous()" />
        <intercept-url pattern="/home" access="permitAll" />
        <intercept-url pattern="/signup" access="permitAll" />
        <intercept-url pattern="/signin" access="permitAll" />
         <intercept-url pattern="/*.auth" access="permitAll" />
        <intercept-url pattern="/**" access="isAuthenticated()" />
        
		<remember-me />
        <form-login username-parameter="email" login-processing-url="/signin/authenticate" password-parameter="passwd" login-page="/signin" authentication-failure-url="/signin" default-target-url="/home" />
        <logout logout-url="/logout" logout-success-url="/home" delete-cookies="JSESSIONID" />
		
	<!--  Spring Social Security authentication filter -->
		<custom-filter ref="socialAuthenticationFilter" before="PRE_AUTH_FILTER" />
		
        <session-management session-fixation-protection="newSession">
             <concurrency-control max-sessions="1" />
         </session-management>
         
         
          <!--   <intercept-url pattern="/**" access="hasRole('ROLE_USER')" />
		<intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')" /> -->
		
    </http>
    
	<authentication-manager alias="authenticationManager">
		<authentication-provider user-service-ref="userDetailsService">
			<password-encoder ref="passwordEncoder" />
		</authentication-provider>
		
		<!-- Spring Social Security authentication provider -->
		<authentication-provider ref="socialAuthenticationProvider" />
	</authentication-manager>	
	
	<jdbc-user-service id="userDetailsService" 
					data-source-ref="dataSource" 
					users-by-username-query="select email, password, true from Account where email = ?"
					authorities-by-username-query="select email, 'ROLE_USER' from Account where email = ?"/>
					
	<beans:bean id="textEncryptor" class="org.springframework.security.crypto.encrypt.Encryptors"
		factory-method="noOpText" />
		
	<beans:bean id="passwordEncoder" class="org.springframework.security.crypto.password.NoOpPasswordEncoder"
		factory-method="getInstance" />
		
  <!-- <authentication-manager>
        <authentication-provider>
            <user-service>
                <user name="admin" password="admin" authorities="ROLE_USER, ROLE_ADMIN"/>
                <user name="user" password="user" authorities="ROLE_USER"/>
            </user-service>
        </authentication-provider>
    </authentication-manager>
     -->  
</beans:beans>